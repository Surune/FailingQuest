public class BufIntensityType
{
    public BufType bufType;
    public int intensity = 0;

    public BufIntensityType(BufType bufType, int intensity)
    {
        this.bufType = bufType;
        this.intensity = intensity;
    }
}